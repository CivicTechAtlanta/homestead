service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{user} {
      allow read, update, delete: if request.auth.uid == resource.__id__;
      allow create: if request.auth.uid != null;
    }
  }
}
